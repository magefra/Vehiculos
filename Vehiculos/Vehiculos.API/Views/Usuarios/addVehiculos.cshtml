  
@model Vehiculos.API.Models.VehiculoViewModel

@{
    ViewData["Title"] = "Create";
}

<h2>Crear</h2>

<h4>Vehiculo</h4>
<hr />
<div class="row">
    <div class="col-md-12">
        <form asp-action="addVehiculos" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdUsuario" />

            <partial name="_Vehiculo" />

            <div class="form-group">
                <label asp-for="ImagenFile" class="control-label"></label>
                <input asp-for="ImagenFile" type="file" class="form-control" />
                <span asp-validation-for="ImagenFile" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
                <a asp-action="Details" asp-route-id="@Model.IdUsuario" class="btn btn-success">Regresar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}